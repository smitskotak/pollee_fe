import 'package:flutter/material.dart';

class ManagePollsListScreen extends StatelessWidget {
  const ManagePollsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('MANAGE'),
    );
  }
}
